export {
    type AddPeerResult,
    type ChannelManagerConfig,
    getChannelManager,
    initChannelManager,
    resetChannelManager,
} from "./manager";
export type { Channel, Peer, TypedWebSocket, WebSocketData } from "./types";
